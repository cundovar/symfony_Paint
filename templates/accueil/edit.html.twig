{% extends 'base.html.twig' %}

{% block title %}Edit PageAccueil{% endblock %}

{% block body %}
    <h1>Edit PageAccueil</h1>

    <div class="container">

    {{ include('accueil/_form.html.twig') }}

    <a href="{{ path('app_accueil_index') }}" class="btn btn-warning">retour</a>

    {# {{ include('accueil/_delete_form.html.twig') }} #}
    
    
    </div>
{% endblock %}
